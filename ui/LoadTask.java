package ui;

import javafx.concurrent.Task;
import parser.CsvLoader;

public class LoadTask<Load> extends Task {
    @Override
    public Load call() {
        //CsvLoader loader = new CsvLoader();
        //loader.load();
        //updateProgress(0,);
        return null;
    }
}
